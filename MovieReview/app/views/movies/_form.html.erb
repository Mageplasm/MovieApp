<%= simple_form_for @movie, :html => { :multipart => true } do |f| %>
	<%= f.input :title, label: "Movie Title" %>
	<%= f.input :description %>
	<%= f.input :genre %>
	<%= f.input :date, label: "Release Date" %>
	<%= f.button :submit %>
<% end %>